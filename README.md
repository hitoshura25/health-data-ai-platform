# health-data-ai-platform
An end-to-end pipeline that automatically collects health data from HealthConnect (Android), stores it in a cloud backend, processes it into a narrative text format, and uses this text to fine-tune and query a personal AI model.
